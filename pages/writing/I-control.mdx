import ArticleLayout from '../../components/ArticleLayout'

export const meta = {
  title: 'This is pretty cool',
  description: 'mdx is quite fun',
  createdAt: new Date(2022, 9, 8).getTime(),
}

# H1

stuff

## H2

stuff

### H3

stuff

another paragraph

another paragraph

I **love** using [Next.js](https://nextjs.org/)

```js
console.log('hello world')
```

test `js console.log('test')` test

export default ({ children }) => (
  <ArticleLayout meta={meta}>{children}</ArticleLayout>
)
