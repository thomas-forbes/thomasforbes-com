import ArticleLayout from '../../components/ArticleLayout'

export const meta = {
  title: 'Being tool-agnostic',
  description:
    "Why it's beneficial to always experiment with new & different tools",
  createdAt: new Date(1970, 0, 1).getTime(),
}

export default ({ children }) => (
  <ArticleLayout meta={meta}>{children}</ArticleLayout>
)
