import ArticleLayout from '../../components/ArticleLayout'

export const meta = {
  title: 'What I learned from building Untools',
  description: 'Lessons from building a side-project on my own',
  createdAt: new Date(1970, 0, 1).getTime(),
}

export default ({ children }) => (
  <ArticleLayout meta={meta}>{children}</ArticleLayout>
)
